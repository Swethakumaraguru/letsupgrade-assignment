def sum_of_squares(numbers):
    """
    This function takes in a list of numbers and returns the sum of the squares of all the numbers in the list.
    """
    sum = 0
    for num in numbers:
        sum += num**2
    return sum
